machine:
  ruby:
    version: 2.3.4
database:
  override:
    - echo "Skipping DB section."
test:
  override:
      -  echo "Skipping test section."
deployment:
  production:
    branch: master
    commands:
      - bundle exec cap production deploy