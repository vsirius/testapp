machine:
  ruby:
    version: 2.3.4
test:
  override:
      - nosetests
deployment:
  staging:
    branch: staging
    commands:
      - bundle exec cap staging deploy
  production:
    branch: master
    commands:
      - bundle exec cap production deploy